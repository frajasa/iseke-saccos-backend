# Test profile configuration
spring.datasource.url=jdbc:postgresql://localhost:5454/saccos_db?currentSchema=public
spring.datasource.username=postgres
spring.datasource.password=frajasa
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=none

# Disable scheduled tasks during tests
spring.task.scheduling.pool.size=1

# Disable payment gateways
payment.mpesa.enabled=false
payment.tigopesa.enabled=false
payment.nmb.enabled=false

# Disable mail
spring.mail.host=localhost
spring.mail.port=25

# Reduce logging noise
logging.level.root=WARN
logging.level.tz.co.iseke=INFO
